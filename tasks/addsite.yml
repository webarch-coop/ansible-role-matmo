---
# Get a list of sites to start with?
# URL's is comer seperated
#
- name: Add Matomo site
  shell: "php console site:add --name='' urls=''"
  args:
    executable: /bin/bash
    chdir: "{{ matomo_home }}"
  become: true
  become_user: "{{ matomo_user }}"
  tags:
    - matomo
